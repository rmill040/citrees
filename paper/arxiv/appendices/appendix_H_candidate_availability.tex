\section{Candidate feature coverage}
\label{app:candidate-availability}

This appendix reports the extended candidate feature coverage analysis separately
from the benchmark-stability summaries in \cref{app:benchmark-stability}.

\Cref{fig:candidate-dimension-curves} extends the main text candidate feature
sampling figure from one sparse classification setting to $p = 100, 500$, and
$1{,}000$ at 1,000 trees. For each method and number of features, the point
marks the mean over $n_{\mathrm{informative}} \in \{1,2,5,10\}$ and the
capped bar spans the minimum and maximum over those four sparse settings.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{figures/paper_mechanism_grid_forest_classification_dimension_curves_1000trees.png}
  \caption{Classification forest candidate feature sampling by number of
  features at 1,000 trees. Points are means over
  $n_{\mathrm{informative}} \in \{1,2,5,10\}$; capped bars show the
  min--max range over those sparse designs, not uncertainty intervals.}
  \label{fig:candidate-dimension-curves}
\end{figure}

The gap between default CIF and CIF-all widens as the number of
features grows and is largest in the sparsest settings. CIF-all is the CIF
variant with \texttt{max\_features=None}, so every nonconstant candidate feature
can enter Stage~A at each split. This diagnostic varies candidate feature coverage
directly; feature muting and all other tree settings are held fixed. With
two informative features,
the difference in informative split share is 0.600 at $p{=}100$, 0.780 at
$p{=}500$, and 0.910 at $p{=}1{,}000$, while the corresponding reductions in the
number of different uninformative features used by the forest are 70, 264,
and 439.
The gap also remains when the signal is less sparse: with five
informative features, the difference in informative split share is still
0.251, 0.564, and 0.543 as $p$ increases from 100 to 500 to 1,000. These
curves are consistent with the main text conclusion that, in sparse high-$p$
forests, candidate subsampling can make informative features unavailable at many
split decisions.
