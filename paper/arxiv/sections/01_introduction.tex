\section{Introduction}
\label{sec:introduction}

Feature selection is a central primitive in modern tabular machine learning:
practitioners want rankings that are predictively useful, stable across
resamples, and interpretable. These goals are especially sharp in regimes with
many features and comparatively few samples (with $n$ samples and $p$ features;
$p \gg n$), where naive selection rules can overfit to noise.

Decision trees provide an appealing embedding feature selector: the split
structure yields an implicit ranking, and tree-based ensembles scale well in
practice. However, classical CART-style trees choose split variables by
maximizing a data-dependent objective over many candidate thresholds. This
creates a systematic selection bias: even under a complete global null where the
response is unrelated to the covariates, features with more candidate split
points are more likely to produce extreme impurity improvements by chance.
This ``high-cardinality'' selection bias has been documented for variable
importance in random forests and related models \citep{Strobl2007BiasRFVariableImportance},
and it appears immediately at the root in one-split trees.

Conditional inference trees \citep{Hothorn2006UnbiasedRecursivePartitioning}
mitigate this issue by separating variable selection from split-point
optimization via hypothesis testing. At each node, the algorithm first screens
features for association with the response (Stage~A). Only if at least one
feature is significant does it proceed to optimize a split threshold (Stage~B).
The test-based Stage~A step is the natural place to attach finite-sample
error-control statements.

\paragraph{Two roles: screening vs.\ embedding.}
We use Stage~A in two complementary ways. First, we treat fixed-node Stage~A
screening (especially at the root) as a standalone, permutation-calibrated
feature screening rule with finite-sample guarantees in fixed-$B$ mode.
Second, we use Stage~A inside a full recursive tree/forest learner whose split
structure induces a feature ranking. In this second role, later-node and
Stage~B quantities are data-adaptive and are evaluated empirically; the paper
is explicit about which reported ``p-values'' are treated as calibrated versus
algorithmic statistics (\cref{sec:pvalue-scope}).

\paragraph{Contributions.}
This paper presents:
(i)~finite-sample, fixed-node (pre-specified; especially root) guarantees for
Stage~A screening in fixed-$B$ mode under a nodewise \emph{complete} (global)
permutation null, including +1 Monte Carlo p-value super-uniformity and
Bonferroni-based bounds on the probability of splitting and on splitting on any
particular tested feature, without any dependence on a feature's number of
candidate thresholds;
(ii)~a set of practical implementation optimizations for scaling conditional
inference trees and forests, together with a clean scoping of which outputs are
treated as classical p-values versus algorithmic stopping statistics; and
(iii)~a benchmark suite evaluating feature rankings on synthetic ground-truth
datasets and real datasets under a two-stage rank-then-evaluate protocol.
For reproducibility, we provide a reference implementation.

\paragraph{Roadmap.}
\cref{sec:background} reviews selection bias in greedy trees and situates the
conditional-inference framework. \cref{sec:method} defines the Stage~A/Stage~B
procedure and the Monte Carlo p-values used throughout. \cref{sec:theory} states
the fixed-node/root guarantees (with proofs in the appendices). \cref{sec:experiments}
describes the rank-then-evaluate benchmark protocol and empirical comparisons.
\cref{sec:discussion} clarifies scope and limitations, especially the
post-selection/adaptive nature of Stage~B and internal-node tests.
