\section{Fixed-node Stage~A control}
\label{sec:theory}

This section analyzes the exhaustive fixed-$B$ Stage~A feature selection rule at a
fixed node. The theorem proves superuniformity for a single $+1$ Monte Carlo
permutation $p$-value, and the corollary applies Bonferroni correction across the
fixed-node Stage~A tests. In the notation of \cref{sec:stageA-stageB}, this is
the reference case where every feature in $F_t$ is tested with the same fixed
nodewise budget $B$. All probabilities below condition on $(X_t,U)$, where $U$
collects label-independent auxiliary randomness other than the Monte Carlo
permutation draws.

\begin{theorem}[$+1$ permutation $p$-values are superuniform]
\label{thm:plusone-superuniform}
Assume A0.1 through A0.5 from \cref{app:assumptions}. Under the nodewise
complete permutation null at the fixed node $t$, conditional on $(X_t,U)$, the
observed statistic together with the $B$ random-permutation statistics has the
rank distribution of an exchangeable tuple of length $B+1$. Define the
right-tail $+1$ Monte Carlo $p$-value
%
\begin{equation}
  p_{\mathrm{MC}}^{\ge}
  := \frac{1 + \sum_{b=1}^B \ind\{T_b \ge T_{\mathrm{obs}}\}}{B+1}.
\end{equation}
%
Then for every $\alpha \in [0,1]$,
%
\begin{equation}
  \PP(p_{\mathrm{MC}}^{\ge} \le \alpha \mid X_t,U) \le \alpha.
\end{equation}
\end{theorem}

This is the standard exchangeable-rank result for Monte Carlo permutation tests
\citep{NorthCurtisSham2002EmpiricalPValuesMonteCarlo,Ernst2004PermutationMethodsExactInference,HemerikGoeman2018ExactTestingRandomPermutations}.
\Cref{app:permutation-exchangeability,app:proof-plusone} state the
exchangeability condition and give the proof. The left-tail version follows by
applying the same result to $-T$.

\begin{corollary}[Stage~A complete null control at a fixed node]
\label{cor:stageA-global-null}
Assume A0.1 through A0.5 from \cref{app:assumptions}. Under the nodewise complete
null at $t$, let $F_t$ be nonempty and write $m_t = |F_t| \ge 1$. Let Stage~A
reject when
%
\begin{equation}
  \min_{j \in F_t} p_{t,j} \le \alpha_{\mathrm{sel}}/m_t.
\end{equation}
%
The tree-growing rule uses a strict comparison, so its rejection event is a
subset of the nonstrict event above.
%
Then
%
\begin{equation}
  \PP\!\left(\exists j \in F_t : p_{t,j} \le \alpha_{\mathrm{sel}}/m_t \,\middle|\, X_t,U\right)
  \le \alpha_{\mathrm{sel}}.
\end{equation}
\end{corollary}

Combining \cref{thm:plusone-superuniform} with the Bonferroni union bound over
the $m_t$ Stage~A tests gives fixed-node Stage~A control under the complete
null. The result is local: it does not cover partial-null familywise error
control, $p$-values for selected features, or error control for fitted trees and
forests.

\begin{corollary}[Stage~B max-type control at a fixed node]
\label{cor:stageB-maxt}
Assume A0.1 through A0.5 from \cref{app:assumptions} at a fixed node $t$, with the tested feature and the finite candidate set $C$ fixed as functions of $(X_t,U)$ alone, a fixed budget $B$ with no optional stopping, and left-tail ties counted with $\le$. Let $p^{\max}_t$ be the max-type Stage~B $p$-value of \cref{eq:maxt-stat} with column moments computed over all $B+1$ rows. Under the nodewise complete permutation null, conditional on $(X_t,U)$, $\PP(p^{\max}_t \le \alpha \mid X_t,U) \le \alpha$ for every $\alpha\in[0,1]$.
\end{corollary}

The argument reduces to \cref{thm:plusone-superuniform} in four steps. Under the complete null the $B+1$ rows of the impurity matrix $(S_{b,c})$ are exchangeable: row $0$ is the observed labels and rows $1,\dots,B$ are independent uniform permutations of them with the identity adjoined \citep{HemerikGoeman2018ExactTestingRandomPermutations}, and \cref{lem:identity-vs-random} applies verbatim to the vector-valued statistic $c \mapsto S_{b,c}$ in $\mathbb{R}^{K}$, since nothing in its proof uses that the statistic is scalar. Column
standardization uses the mean and standard deviation of all $B+1$ entries of
each column, which are symmetric functions of the rows, so the standardized
matrix has exchangeable rows as well; the exclusion of candidates with an empty
child or zero column variance is likewise a function of the whole column. The
row minimum is one fixed function applied to every row, so $T_0,\dots,T_B$ are
exchangeable. The inclusive-rank $p$-value of an exchangeable tuple is
superuniform by the proof of \cref{thm:plusone-superuniform}, applied to $-T$
for the left tail. The moments must include row $0$: estimating them from the permuted rows alone singles out the observed row, breaks the symmetry the argument uses, and can be anti-conservative. The guarantee is weak familywise control over the $K$ candidates, the probability of declaring any split under the node's global null. It is a fixed-node, fixed-$B$ statement for a feature fixed without reference to $Y_t$; it does not cover the adaptive variant, whose column moments and chosen threshold move with the stopping time, and it is not a post-selection $p$-value for a feature chosen by Stage~A from the same responses.

\begin{proposition}[Exact size of the adaptive stopping rule]
\label{prop:adaptive-size}
Assume A0.1 through A0.3 at a fixed node for one permutation test whose statistic is a fixed function of each permuted response vector (a Stage~A test or a per-threshold Stage~B test), with budget $B$, level $\alpha$, confidence $c$, and batch size $s$. The adaptive rule draws permutations in batches of $s$; at each batch boundary $m \ge \lceil 1/\alpha \rceil$ it stops when the $\mathrm{Beta}(1+k,\,1+m-k)$ posterior places mass at least $c$ on either side of $\alpha$, where $k$ counts permuted statistics at least as extreme as the observed one, and it reports $(k+1)/(m+1)$. Under the complete nodewise null with no ties, $\PP(\text{reported value} \le \alpha \mid X_t,U) = \rho(\alpha,c,s,B)$, the rejection probability of the same rule applied to a P\'olya urn started from one ball of each colour; with ties the probability is at most $\rho$. At the shipped settings $c=0.95$ and $s=32$, $\rho(0.05,0.95,32,999)=0.0495$, $\rho(0.05,0.95,32,1537)=0.0491$, and $\rho\le\alpha$ for every $B$ from 20 to 3{,}000; $\rho(0.05,0.80,32,999)=0.0523$.
\end{proposition}

Under the null the observed and permuted statistics are exchangeable, so after $m$ draws the count of permuted values at least as extreme as the observed one is the number of white balls drawn from a P\'olya urn started with one white and one black ball, and the next indicator is one with probability $(k+1)/(m+2)$. The stop and report decisions are deterministic functions of $(m,k)$ at the batch boundaries, so the rejection event is a union over stopping states whose probabilities a finite recursion computes exactly; \texttt{paper/theory/adaptive\_stopping\_size.py} regenerates the values above and the accompanying test verifies that the implementation's Beta CDF makes the same stopping decision as a reference implementation at every reachable state. The rejection event shrinks when any indicator switches from zero to one, and counting ties as exceedances can only switch indicators in that direction, so the tie-inclusive size is at most $\rho$.

\begin{remark}
Under the \texttt{minimum} budget with Bonferroni correction the floor $\lceil 1/\alpha_{\mathrm{adj}}\rceil$ equals the budget, so the first stopping check coincides with exhaustion and the rule reduces to the fixed-$B$ test of \cref{thm:plusone-superuniform}. This is the benchmark configuration: every Stage~A and per-threshold Stage~B $p$-value in the benchmark is a fixed-$B$ $+1$ permutation $p$-value. The adaptive max-type kernel recomputes its column moments over all permutations drawn so far at each check, so its exceedance path is not a P\'olya urn and \cref{prop:adaptive-size} does not cover it; its calibration rests on the complete-node simulations.
\end{remark}
