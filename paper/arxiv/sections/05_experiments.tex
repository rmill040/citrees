\section{Experiments}
\label{sec:experiments}

This section defines our benchmark protocol and analysis-plan contract for
synthetic ground-truth datasets and real tabular datasets. The empirical goal is
to evaluate methods as \emph{feature rankers} in a way that separates ranking
quality from downstream model choice. Until the results tables/figures are
populated, this section should be read as a pre-results specification rather than
as evidence for method superiority.

\subsection{Protocol: rank-then-evaluate}
\label{sec:experiments-protocol}

We treat each method as a \emph{ranker} and use a two-stage protocol:
(i)~produce a feature ranking on a training split, and
(ii)~train downstream models on the top-$k$ selected features and evaluate on
held-out data. This protocol isolates ranking quality from downstream model
choice and avoids leakage by fitting all preprocessing on the training portion
only. We use 5-fold cross-validation repeated over 10 random seeds and evaluate
$k \in \{5,10,25,50,100,p\}$ (clipped to $k \le p$). Details and notation are in
\cref{app:methods-benchmark}.

\subsection{Primary endpoints and analysis plan (pre-specified)}
\label{sec:experiments-analysis-plan}

Because the protocol produces results across multiple $k$ values, downstream
models, and metrics, we pre-specify primary endpoints to reduce degrees of
freedom in the headline comparisons.

\paragraph{Primary endpoints (real datasets).}
For classification, the primary endpoint is balanced accuracy of logistic
regression trained on the top-$k$ features with $k=25$. For regression, the
primary endpoint is $R^2$ of ridge regression trained on the top-$k$ features
with $k=25$. All other $k$ values, downstream models, and metrics are treated as
secondary and reported as robustness checks.

\paragraph{Aggregation.}
For each dataset and method, we first average the primary endpoint across folds
and random seeds. Cross-dataset summaries are computed from these dataset-level
means. We report (i)~average ranks across datasets, and (ii)~paired, per-dataset
differences versus \citrees (median and interquartile range). Formal multiple
comparison procedures and uncertainty summaries will be locked to the final
analysis script configuration and reported in the results tables before any
headline empirical claim is made.

\paragraph{Synthetic ground truth.}
For synthetic datasets with known informative feature sets, the primary ranking
metrics are precision@k, recall@k, and F1@k at $k\in\{5,10,25,50,100\}$ (with
$k$ clipped to $k\le p$), together with noise-selection diagnostics in
null-style regimes. These definitions match \cref{app:methods-benchmark}.

\subsection{Datasets and preprocessing}
\label{sec:experiments-datasets}

All datasets are stored as Parquet files under \texttt{paper/data/}.

\paragraph{Synthetic ground truth.}
We use curated synthetic suites with known informative feature sets:
169 classification datasets (\texttt{paper/data/classification/synthetic/}) and
163 regression datasets (\texttt{paper/data/regression/synthetic/}). The suites
include regimes designed to probe selection bias (e.g., high-cardinality noise),
nonlinear dependence, correlated feature blocks, redundancy, high-dimensional
scaling, and weak-signal settings.

\paragraph{Real datasets.}
We evaluate on 24 real classification datasets
(\texttt{paper/data/classification/real/}) and 8 real regression datasets
(\texttt{paper/data/regression/real/}), emphasizing tabular feature selection
regimes including $p \gg n$.

\paragraph{Preprocessing and leakage prevention.}
All preprocessing is fit on the training portion only within each fold and then
applied to the held-out portion. We standardize features using training-fold
means and variances; see \cref{app:methods-benchmark} for the code-aligned
definition. A dataset summary table (provenance and $n/p$ statistics) will be
added in the finalized version.

\subsection{Methods and fairness policy}
\label{sec:experiments-methods}

We compare four families of feature selectors:
(i)~\emph{filters} based on univariate dependence scores (e.g., correlation,
mutual information, RDC, mRMR);
(ii)~\emph{permutation-calibrated filters} that convert filter scores into
permutation p-values;
(iii)~\emph{embedded} selectors from tree and ensemble learners (including
\emph{conditional-inference} trees/forests (the procedure studied here) and
strong CART-style baselines); and
(iv)~\emph{wrappers} such as Boruta, permutation importance variants, and RFE. Implementation-aligned definitions of the selector and
splitter statistics used in our experiments are in
\cref{app:methods-selectors,app:methods-splitters}.

\paragraph{Hyperparameters and compute budgets.}
To avoid optimistic bias from unequal tuning effort, each baseline is evaluated
under a pre-specified hyperparameter grid and compute budget. The final version
will include the full method roster, grids, and failure/timeout policy aligned
to the experiment pipeline configuration.

\subsection{Results (placeholders; populated after runs complete)}
\label{sec:experiments-results}

This subsection describes the figures/tables produced by the benchmark suite
and states the pre-specified analysis questions. Figure panels and tables will
be inserted after the experiment runs complete. Empirical-effect and
significance claims are deferred until those artifacts are populated.

\subsubsection{Selection bias under an exchangeable complete global null}
\label{sec:experiments-selection-bias}

\begin{figure}[t]
  \centering
  \fbox{\begin{minipage}[c]{0.95\linewidth}
    \centering
    \vspace{0.55in}
    \textbf{Figure placeholder.}\par
    TODO: Insert the selection-bias demo figure (complete global null; high-cardinality noise).\par
    \vspace{0.55in}
  \end{minipage}}
  \caption{Selection bias in greedy split optimization under a complete global null (label exchangeability given covariates) and the effect of fixed-$B$ Stage~A screening at the root (Bonferroni).
  (TODO: replace placeholder with the generated figure and finalize caption details.)}
  \label{fig:selection-bias-demo}
\end{figure}

\noindent\textbf{Planned analysis question.} Under a complete global null, do
greedy threshold-search baselines favor high-cardinality features, and does
fixed-$B$ Stage~A screening reduce root-level false splits in this setting?

\subsubsection{Synthetic ground-truth ranking quality}
\label{sec:experiments-synthetic}

\begin{figure}[t]
  \centering
  \fbox{\begin{minipage}[c]{0.95\linewidth}
    \centering
    \vspace{0.55in}
    \textbf{Figure placeholder.}\par
    TODO: Insert synthetic ranking-quality summary (precision/recall/F1 vs $k$).\par
    \vspace{0.55in}
  \end{minipage}}
  \caption{Ranking quality on synthetic datasets with known informative-feature sets.
  (TODO: replace placeholder with the generated figure and finalize caption details.)}
  \label{fig:synthetic-ranking}
\end{figure}

\noindent\textbf{Planned analysis question.} How do methods compare in ranking
known informative features above noise across $k$ values and stress-test regimes
(correlated blocks, redundancy, high-dimensional settings, weak signals)?

\subsubsection{Runtime and scalability}
\label{sec:experiments-runtime}

\begin{figure}[t]
  \centering
  \fbox{\begin{minipage}[c]{0.95\linewidth}
    \centering
    \vspace{0.55in}
    \textbf{Figure placeholder.}\par
    TODO: Insert runtime/speedup curves (fixed-$B$ vs heuristics; parallelism).\par
    \vspace{0.55in}
  \end{minipage}}
  \caption{Runtime impact of permutation testing and practical speedups (early stopping, scanning, and parallelism).
  (TODO: replace placeholder with the generated figure and finalize caption details.)}
  \label{fig:runtime}
\end{figure}

\noindent\textbf{Planned analysis question.} What is the runtime cost of
permutation-based screening, and which optimizations reduce runtime with minimal
loss in ranking quality?

\subsubsection{Real-data performance (rank-then-evaluate)}
\label{sec:experiments-real}

\begin{figure}[t]
  \centering
  \fbox{\begin{minipage}[c]{0.95\linewidth}
    \centering
    \vspace{0.55in}
    \textbf{Figure placeholder.}\par
    TODO: Insert real-data summary (primary endpoint + robustness across $k$/models).\par
    \vspace{0.55in}
  \end{minipage}}
  \caption{Real-data downstream performance using top-$k$ features produced by each ranker.
  (TODO: replace placeholder with the generated figure and finalize caption details.)}
  \label{fig:real-data}
\end{figure}

\noindent\textbf{Planned analysis question.} Does ranking behavior on synthetic
ground truth translate to competitive downstream performance on real tabular
datasets under the pre-specified primary endpoints?

\paragraph{Reproducibility.}
Experiment orchestration, analysis, and figure generation scripts live under
\texttt{paper/scripts/}, and generated artifacts are written to
\texttt{paper/results/}. The repository is available at
\url{https://github.com/rmill040/citrees}. The final arXiv bundle will vendor
all paper figures/tables used for empirical claims.
