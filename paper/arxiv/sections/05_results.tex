\section{Benchmark results and ablations}
\label{sec:results}

\subsection{Real-data benchmark}
\label{sec:experiments-real}

\Cref{tab:benchmark-summary} summarizes the real-data benchmark results.

\begin{table}[H]
  \centering
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \renewcommand{\arraystretch}{1.12}
  \begin{tabular*}{\linewidth}{@{\extracolsep{\fill}}lcccccccc@{}}
    \toprule
    Benchmark & Datasets & CIF score &
    \multicolumn{3}{c}{Mean delta vs. baseline} &
    \multicolumn{3}{c}{Wins vs. baseline} \\
    \cmidrule(lr){4-6}\cmidrule(l){7-9}
    & & & \mbox{r\_ctree} & \mbox{r\_cforest} & CIT &
    \mbox{r\_ctree} & \mbox{r\_cforest} & CIT \\
    \midrule
    Full classification & 22 & BA 0.819 & +0.088 & +0.072 & +0.030 &
    22/22 & 19/22 & 21/22 \\
    14-dataset classification & 14 & BA 0.798 & +0.109 & +0.111 & +0.041 &
    14/14 & 13/14 & 13/14 \\
    Regression & 8 & $R^2$ 0.329 & +0.226 & +0.721 & +0.360 &
    7/8 & 7/8 & 6/8 \\
    \bottomrule
  \end{tabular*}
  \caption{Real-data benchmark summary. Deltas are CIF minus baseline in
  balanced accuracy for classification and $R^2$ for regression. Positive
  deltas favor CIF.}
  \label{tab:benchmark-summary}
\end{table}

On the 14-dataset classification benchmark, the paired
95\% bootstrap intervals for the
mean CIF-minus-baseline deltas are [0.068, 0.154] for \mbox{r\_ctree},
[0.067, 0.161] for \mbox{r\_cforest}, and [0.023, 0.060] for CIT. This
same 14-dataset benchmark also favors CIF overall
($\chi^2(14)=125.73$, $p<0.001$, Kendall's $W=0.64$).
CIF consistently outperforms the
conditional-inference baselines on this benchmark. Under the same 14-dataset classification
aggregation, leave-one-dataset-out reselection moves CIF only from 5th of 15 to
tied 5th--6th of 15 methods and reselects the same CIF configuration on all 14
held-out datasets, indicating that the main classification result is not an
artifact of a single global CIF configuration choice.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/k_trajectory.png}
  \caption{CIF mean-rank trajectory across feature budgets on the
  classification benchmark. Rank improves as $k$ grows into the intermediate
  budgets, while complete-case support shrinks from 22 datasets at $k{=}5$ to 14
  at $k{=}100$.}
  \label{fig:k-trajectory}
\end{figure}

\Cref{fig:k-trajectory} shows how CIF's mean rank changes with feature budget. CIF is
not strongest at the smallest budgets; its mean rank improves as the budget expands
toward the intermediate budgets. The full classification benchmark complements the
14-dataset benchmark by showing that CIF benefits from moving beyond the smallest
budgets.

\FloatBarrier

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/benchmark_pairwise_sensitivity.png}
  \caption{Classification downstream-model $\times$ $k$ sensitivity for CIF
  against the historical conditional-inference references and CIT. Each cell
  is the mean CIF-minus-baseline balanced-accuracy delta for one
  downstream-model and feature-budget combination before within-dataset
  pooling. All 15 classification cells are positive.}
  \label{fig:benchmark-pairwise-sensitivity}
\end{figure}

\Cref{fig:benchmark-pairwise-sensitivity} breaks the full classification
benchmark out by downstream model and feature budget for the three historical
conditional-inference references. CIF is
positive against \mbox{r\_ctree}, \mbox{r\_cforest}, and CIT in all 15
classification downstream-model $\times$ $k$ cells. The CIF-versus-CIT margin
is small at $k \in \{5,10\}$ and grows at the intermediate budgets, while the
CIF-versus-\mbox{r\_ctree} and CIF-versus-\mbox{r\_cforest} margins are
already positive throughout and are usually largest from $k=25$ onward. This
is the main evidence behind the pooled classification benchmark summary.

CIF's dataset-level positions remain broad and stable. On the full
classification benchmark, CIF lands in the top
half on 21 of 22 datasets and in the top 3 on 7 of 22, with per-dataset
positions ranging from 1st to tied 8th--9th. On regression, it stays in the
top half on all 8 datasets and in the top 3 on 2 of 8, ranging from 1st to
8th. These breadth summaries match the pairwise picture: CIF beats
\mbox{r\_ctree}, \mbox{r\_cforest}, and CIT on 22/22, 19/22, and 21/22
classification datasets and on 7/8, 7/8, and 6/8 regression datasets.

\paragraph{Regression benchmark.}
On the regression benchmark, CIF is 2nd of 16 overall, stays in the top half on
all 8 datasets, and outperforms the historical conditional-inference baselines
and CIT. This smaller benchmark supports the same overall direction as the
classification results, but on a thinner evidence base.

\subsection{CIF hyperparameter ablations}
\label{sec:results-practical}

These comparisons change one CIF hyperparameter at a time to show how each
choice affects runtime and scores. Adaptive stopping and histogram
thresholding have the largest effect on runtime. Once adaptive stopping is
enabled, feature scanning adds only a modest further gain. We vary adaptive
stopping, histogram thresholding, feature scanning, Bonferroni correction,
threshold scanning, muting, and bootstrap in matched CIF-only comparisons.
These ablations compare CIF against itself rather than rerun the full
benchmark.

\begin{table}[H]
  \centering
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabularx}{\linewidth}{@{}>{\raggedright\arraybackslash}p{0.34\linewidth} >{\raggedright\arraybackslash}p{0.16\linewidth} >{\raggedright\arraybackslash}p{0.17\linewidth} >{\raggedright\arraybackslash}X@{}}
    \toprule
    Ablation & Slowdown & Max score change & Max recovery change \\
    \midrule
    Disable adaptive stopping &
    5.1--11.7$\times$ &
    $\le 0.0051$ &
    $\le 0.0052$ \\
    Histogram-256 $\rightarrow$ exact threshold search &
    1.9--10.6$\times$ &
    $\le 0.010$ &
    $\le 0.017$ \\
    Disable feature scanning &
    1.02--1.22$\times$ &
    $\le 0.0055$ &
    $\le 0.0095$ \\
    \bottomrule
  \end{tabularx}
  \caption{Main CIF hyperparameter ablations. Slowdowns are relative to the
  default CIF setting. Score changes use downstream balanced accuracy or
  $R^2$. Recovery changes use synthetic informative-feature recovery. Each
  change is the largest absolute difference seen when one CIF hyperparameter
  is changed and the rest of the setup is kept the same.}
  \label{tab:practical-controls}
\end{table}

Numerically, disabling adaptive stopping is 5.1--11.7$\times$ slower with
near-zero downstream score change. Replacing histogram-256 with exact
threshold search is 1.9--10.6$\times$ slower with similarly small downstream
score changes. With adaptive stopping held fixed, removing feature scanning is
1.02--1.22$\times$ slower across the matched datasets, while downstream score
changes stay between about $-0.0055$ and $+0.0006$. Removing Bonferroni correction speeds up CIF
but makes trees much deeper. The other ablations are less
consistent. Removing threshold scanning changes runtime by
0.69--1.07$\times$. Its downstream score changes are small. Muting has
almost no effect on runtime: 0.97--1.00$\times$. Its downstream
score changes are negligible. Removing bootstrap changes depth and feature use
without a consistent speed change. Runtime ranges from 0.89--1.36$\times$, and downstream
score changes are mixed. A smaller histogram budget can reduce runtime further.
The histogram-256 default remains much faster than exact threshold search.

\section{Where CIF Weakens}
\label{sec:boundary}

The additional analyses identify three patterns. On the high-$p$ classification
datasets,
CIF often performs best at intermediate feature budgets. Its selected top-$k$
sets are stronger than its single top-ranked feature, and it weakens when
forest subsampling keeps informative features out of the candidate set too often.

\paragraph{High-$p$ real-data results.}
Across the 15 classification datasets with more than 100 features, CIF first
reaches its best score between 100 and $p$ in 30 of 45 dataset and model
combinations. Only 5 of 45 first do so at $k{=}p$. \Cref{fig:high-p-boundary}
makes the classification boundary explicit by downstream learner. Intermediate
budgets dominate for all three downstream models: 11 of 15 LR cells, 9 of 15
SVM cells, and 10 of 15 KNN cells first reach their best score at
$100 < k < p$. Endpoint budgets are mainly an LR exception. For LR, moving
from $k{=}100$ to $k{=}p$ improves CIF on average by $+0.022$ and the endpoint
matches the best observed score in 6 of 15 cells. For SVM and KNN, the
corresponding mean changes are $-0.058$ and $-0.075$, and the endpoint is best
in only 1 of 15 cells each.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/high_p_boundary_summary.png}
  \caption{Classification high-$p$ boundary for CIF by downstream learner.
  Left: share of high-$p$ classification cells where the first best observed
  CIF budget falls below 100, at 100, between 100 and $p$, or at the endpoint.
  Right: mean balanced-accuracy change from $k{=}100$ to $k{=}p$.}
  \label{fig:high-p-boundary}
\end{figure}

The regression results are smaller and less favorable. Across the 6
regression datasets with more than 100 features, 11 of 18 dataset and model
combinations peak at or below $k{=}100$, only 2 peak at the endpoint, and the
mean endpoint-minus-$k{=}100$ score change is $-0.308$. Using all features is
therefore usually not helpful, and the endpoint exception is mainly a
classification-LR phenomenon rather than a general CIF pattern.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/synthetic_topk_focus_curves.png}
  \caption{Synthetic top-$k$ composition curves for CIF, CIT, RF, and ET on the
  classification and regression synthetic datasets. As $k$ grows, the share of informative features falls
  and the share of pure-noise features rises. On redundant datasets, CIF still selects
  signal or redundant features rather than pure noise.}
  \label{fig:synthetic-topk-curves}
\end{figure}

\paragraph{Synthetic feature recovery.}
Across the classification synthetic datasets, CIF ranks 8th of 15 by the share
of informative features selected. Its top-1 hit rate is 0.64, which places it
tied 11th--12th of 15 by top-1 hit rate. CIF is better at building a useful top-$k$ set than
at finding the exact top-ranked feature. \Cref{tab:synthetic-head}
summarizes this contrast, and \Cref{fig:synthetic-topk-curves}
shows how the selected set changes as $k$ grows. The share of
informative features in the selected set falls as $k$ grows. On the redundant classification synthetic datasets, 83\% of the selected
features are still signal or redundant rather than pure noise.

\begin{table}[t]
  \centering
  \small
  \setlength{\tabcolsep}{8pt}
  \begin{tabular}{@{}l r r l@{}}
    \toprule
    Task & Mean informative-share rank & Top-1 hit rate & Top-1 hit-rate rank \\
    \midrule
    Classification & 8 of 15 & 0.64 & tied 11--12 of 15 \\
    Regression & 10 of 16 & 0.81 & 13 of 16 \\
    \bottomrule
  \end{tabular}
  \caption{Synthetic feature-recovery summary for CIF. The informative-share
  rank orders methods by the mean share of true informative features selected
  over $k = 5, 10, 25, 50$, and $100$. The top-1 hit rate is the fraction of
  synthetic runs where the first-ranked feature is truly informative.}
  \label{tab:synthetic-head}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/paper_mechanism_grid_forest_classification_feature_counts_p1000_i2_1000trees.png}
  \caption{Feature-count distribution in the forest on the fixed sparse
  classification design with $n{=}250$, $p{=}1000$, two informative features,
  and 1000 trees. Default CIF spreads splits over many false features,
  while \texttt{cif\_all} recovers the informative pair more cleanly.}
  \label{fig:mechanism-forest-counts}
\end{figure}

\paragraph{Candidate-set exposure.}
The simulation study helps explain this limit. In single trees, CIT selects
informative splits at rates of 1.000 on classification and 0.935 on regression. Stage~A works well when
all features are available. Most of the loss in informative-split selection appears after forest subsampling. On
the 1000-tree classification simulations, default CIF selects informative splits 33.2\%
of the time, while \texttt{cif\_all} reaches 91.7\%, RF reaches 8.4\%, and ET reaches 6.5\%.
Default CIF also uses an average of 265 distinct false features, versus 93 for
\texttt{cif\_all}. In regression,
\texttt{cif\_all} selects informative splits 42.3\% of the time, versus 27.8\%
for default CIF. It also uses more distinct false features:
528.8 versus 367.7. Conditional-inference feature selection works when
informative features enter the candidate set often enough. In sparse high-$p$ settings,
forest subsampling can leave them out of the candidate set too often at a node.
