\documentclass[article]{jss}

\usepackage{amsmath,amssymb,booktabs,capt-of,lmodern,needspace,thumbpdf,xspace}

\newcommand{\citrees}{\pkg{citrees}\xspace}
\newcommand{\sklearn}{\pkg{scikit-learn}\xspace}

\author{Robert Milletich\\Amazon Web Services
  \And Justin Downes\\Amazon Web Services
  \And Newel Hirst\\Amazon Web Services}
\Plainauthor{Robert Milletich, Justin Downes, Newel Hirst}

\title{\citrees: Conditional Inference Trees and Forests for \proglang{Python}}
\Plaintitle{citrees: Conditional Inference Trees and Forests for Python}
\Shorttitle{\citrees: Conditional Inference Trees and Forests}

\Abstract{
  Conditional inference trees separate variable selection from split-point
  optimization, reducing the advantage that variables with many possible
  cutpoints receive in greedy tree construction. \citrees is an open-source
  \proglang{Python} implementation of conditional-inference-style trees and
  forests for classification and regression. The package provides estimators
  compatible with common \sklearn workflows while exposing the statistical
  and computational choices used at each node. These choices include linear
  and nonlinear association statistics, Monte Carlo permutation tests,
  multiplicity adjustment, sequential stopping, bounded threshold searches,
  feature muting, bootstrap sampling, and separate samples for structure and
  leaf estimation. This article describes the statistical procedure and its
  inferential scope, the software architecture and estimator interface, and
  the relationship to \pkg{partykit} and CART implementations. Accompanying
  replication analyses evaluate null calibration, split-variable selection,
  matched behavior across implementations, and computational scaling.
}

\Keywords{conditional inference trees, random forests, permutation tests,
  feature selection, machine learning, \proglang{Python}}
\Plainkeywords{conditional inference trees, random forests, permutation tests,
  feature selection, machine learning, Python}

\Address{
  Robert Milletich, Justin Downes, Newel Hirst\\
  Amazon Web Services\\
  United States\\
  E-mail: \email{rmilleti@amazon.com}, \email{jusdow@amazon.com},
  \email{nhirst@amazon.com}
}

\begin{document}

\section{Introduction} \label{sec:introduction}

Classification and regression trees divide a predictor space into regions that
can be represented by a sequence of simple decisions. In the CART procedure,
the split variable and threshold are selected together by optimizing an
impurity criterion \citep{Breiman+Friedman+Olshen+Stone:1984}. Variables with
more admissible cutpoints consequently receive more opportunities to produce a
favorable split under noise. This mechanism is a documented source of
split-variable selection bias in trees and forest importance measures
\citep{Strobl+Boulesteix+Zeileis+Hothorn:2007}.

Conditional inference trees separate the two decisions. A node first tests
predictor-response association and selects a variable, then searches for a
threshold only within that variable
\citep{Hothorn+Hornik+Zeileis:2006}. The framework is implemented in the
\proglang{R} package \pkg{partykit}, which also supplies infrastructure for
recursive partitioning models \citep{Hothorn+Zeileis:2015}. In
\proglang{Python}, \sklearn provides mature CART trees and forests and a common
estimator interface for model selection and pipelines
\citep{Pedregosa+Varoquaux+Gramfort:2011}. \citrees extends this estimator
ecosystem with native conditional inference classifiers and regressors. The
package separates nodewise feature screening from threshold search, then adds
selector choices, threshold approximations, sequential computation, forest
sampling controls, and model inspection methods designed for
\proglang{Python} workflows. Each statistical and computational choice is an
explicit fitted-estimator parameter.

\section{Statistical procedure} \label{sec:procedure}

Let $(X_t, y_t)$ denote the observations reaching node $t$, and let
$F_t$ be the nonconstant predictors considered at that node after any feature
sampling or muting. Tree construction has two stages. Stage A tests each
predictor in $F_t$ for association with the response. Stage B searches
thresholds for the predictor selected by Stage A. Either stage can stop the
node without a split.

\subsection{Nodewise feature screening} \label{sec:screening}

For predictor $j$, let $T_j$ be its configured association statistic and let
$T_{j,1},\ldots,T_{j,B}$ be the same statistic after $B$ random permutations
of the response. \citrees uses the corrected Monte Carlo value
%
\begin{equation}
  p_j =
  \frac{1 + \sum_{b=1}^{B}
    \mathbb{1}\!\left\{T_{j,b} \geq T_j\right\}}
       {B + 1},
  \label{eq:permutation-p}
\end{equation}
%
whose minimum attainable value is $1/(B+1)$
\citep{Phipson+Smyth:2010}. With multiplicity adjustment enabled, predictor
$j$ passes the nodewise screen when $p_j < \alpha_{\mathrm{A}}/|F_t|$.
The per-candidate permutation budget is recomputed at each node from this
adjusted level. Named minimum, automatic, and maximum settings use
$\alpha_{\mathrm{A}}/|F_t|$, while a numeric budget is multiplied by
$|F_t|$. The minimum setting therefore uses
$\lceil |F_t|/\alpha_{\mathrm{A}} \rceil$ permutations, the first budget that
permits a corrected value below the adjusted gate. With adjustment disabled,
the threshold is $\alpha_{\mathrm{A}}$. The passing predictor with the smallest
value is selected. Exact ties among evaluated candidates are resolved by
one-pass reservoir sampling, which gives each tied candidate equal probability
under the fitted random seed.

Classification selectors include multiple correlation, mutual information,
and the randomized dependence coefficient. Regression selectors include
Pearson correlation, distance correlation, and the randomized dependence
coefficient \citep{Szekely+Rizzo+Bakirov:2007,Lopez-Paz+Hennig+Schoelkopf:2013}.
Compatible bounded statistics can also be combined in a max-$T$ permutation
test \citep{Westfall+Young:1993}. This option evaluates the largest configured
selector score within each response permutation and uses the resulting joint
max-statistic reference distribution.
The RDC implementation uses 10 random projections (20 sine and cosine columns).
A 5-seed, 5-fold sensitivity analysis compared 5, 10, 20, and 40 projections on
Wine (178 samples, 13 features), Glass (214 samples, 10 features), Heart
Statlog (270 samples, 13 features), and Parkinsons (195 samples, 22 features).
Across 25 paired seed-fold evaluations per dataset, median selector time with
20 projections was 3.00 to 3.06 times that with 10. At reduced feature counts
(5 and 10 for Wine, Heart Statlog, and Parkinsons; 5 for Glass), mean balanced
accuracy differences (20 minus 10) ranged from $-0.0052$ to $0.0005$ across the
three downstream classifiers, and mean overlap between selected feature sets
ranged from 95.4\% to 99.8\%. Using 10 projections reduced median selector time
by 66.6\% to 67.4\% relative to 20 while preserving the observed accuracy at
these scales.

The exhaustive procedure evaluates every candidate with the requested
permutation budget and reports fixed-$B$ $p$~values. Before sequential feature
testing, candidates are sampled without replacement under the fitted random
seed. Feature scanning reorders that sample by the observed selector statistic.
Adaptive stopping checks its posterior criterion every 32 permutations; simple
stopping checks significance and futility after each permutation beyond the
minimum budget. Both sequential modes can stop at the first candidate whose
value passes the adjusted gate. The fitted estimator reports the resulting
decision and realized selector and threshold permutation counts.

\subsection{Threshold selection} \label{sec:thresholds}

After Stage A selects predictor $j^\star$, Stage B constructs candidate
thresholds from the ordered unique values of $X_{t,j^\star}$. The exact method
uses all admissible midpoints. Random, percentile, and histogram methods
construct bounded subsets for larger nodes. Candidates that violate the
minimum leaf size are removed before evaluation.

Each remaining candidate is scored by the weighted impurity of its left and
right children. Classification supports Gini and entropy criteria, while
regression supports squared and absolute error. The candidate score can be
tested with response permutations using a left-tail counterpart of
Equation~\ref{eq:permutation-p}. If no candidate passes the configured Stage B
threshold, the node is terminal. The accepted threshold is a tree-construction
decision after feature selection. Calibration analyses evaluate the fixed-node
permutation gates. Stage B applies the same per-node Bonferroni budget rule
across admissible thresholds. During sequential fitting, threshold scanning
orders candidates by weighted impurity; otherwise their order is randomized
under the fitted seed.

\subsection{Forests and feature summaries} \label{sec:forests}

A conditional inference forest fits trees to bootstrap samples and can sample
a subset of predictors at each node. Classification forests optionally use
stratified, under-, or oversampling when constructing bootstrap samples.
Predictions average class probabilities or regression estimates across trees.
Out-of-bag scores are available when bootstrapping is enabled.

Tree feature importances sum the local impurity decreases attributed to each
accepted split and normalize the result when its total is positive. Forest
importances aggregate the normalized tree vectors. Stage A therefore controls
which predictors may enter threshold optimization, while the reported
importance measures the impurity reduction of accepted splits. The package
also exposes terminal-node indices and sparse decision paths for direct model
inspection.

\subsection{Scope of statistical interpretation} \label{sec:scope}

For a fixed node, a fixed predictor set, and an exhaustive fixed permutation
budget, Equation~\ref{eq:permutation-p} has the usual exchangeability-based
Monte Carlo interpretation. A fitted tree adds response-dependent node
creation, repeated tests, optional feature ordering and muting, and threshold
selection. Forests add bootstrap samples, predictor sampling, and aggregation.
Equation~\ref{eq:permutation-p} defines the fixed-node permutation test used
during tree construction. The fitted tree and forest analyses target predictive
behavior, feature stability, and computation under adaptive construction.

\section{Software design and interface} \label{sec:software}

\citrees is implemented in \proglang{Python} and distributes four public
estimators: tree and forest variants for classification and regression. The
estimators implement \code{fit()} and \code{predict()}, classifiers implement
\code{predict_proba()}, and fitted models expose \code{apply()},
\code{decision_path()}, and \code{feature_importances_}. They can therefore
participate directly in \sklearn cross-validation, pipeline, and
parameter-search utilities.

\Needspace{18\baselineskip}
\subsection{Executable tutorial} \label{sec:tutorial}

The executable tutorial uses the Breast Cancer Wisconsin Diagnostic data
\citep{Wolberg+Mangasarian+Street+Street:1993}, with 569 observations and 30
continuous predictors. A stratified split assigns 426 observations to training
and 143 to evaluation. The following code fits a tree and forest, obtains
predictions, and inspects the fitted tree.
%
\begin{Code}
import numpy as np
from citrees import (ConditionalInferenceForestClassifier,
                     ConditionalInferenceTreeClassifier)
from sklearn.datasets import load_breast_cancer
from sklearn.metrics import balanced_accuracy_score, roc_auc_score
from sklearn.model_selection import train_test_split

data = load_breast_cancer(as_frame=True)
X, y = data.data, data.target
X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=0.25, stratify=y, random_state=1718
)

common = dict(
    selector="mc",
    n_resamples_selector="minimum",
    n_resamples_splitter="minimum",
    threshold_method="histogram",
    max_thresholds=32,
    random_state=1718,
)
tree = ConditionalInferenceTreeClassifier(max_depth=3, **common)
tree.fit(X_train, y_train)
tree_predictions = tree.predict(X_test)
tree_probabilities = tree.predict_proba(X_test)[:, 1]
tree_balanced_accuracy = balanced_accuracy_score(
    y_test, tree_predictions
)
tree_roc_auc = roc_auc_score(y_test, tree_probabilities)
tree_path = tree.decision_path(X_test)
tree_leaves = tree.apply(X_test)
leading_order = np.argsort(-tree.feature_importances_)[:5]
leading = tree.feature_names_in_[leading_order]

forest = ConditionalInferenceForestClassifier(
    n_estimators=100,
    max_depth=5,
    bootstrap=True,
    oob_score=True,
    n_jobs=1,
    **common,
)
forest.fit(X_train, y_train)
forest_predictions = forest.predict(X_test)
forest_probabilities = forest.predict_proba(X_test)[:, 1]
forest_balanced_accuracy = balanced_accuracy_score(
    y_test, forest_predictions
)
forest_roc_auc = roc_auc_score(y_test, forest_probabilities)
forest_oob_accuracy = forest.oob_score_
forest_leaves = forest.apply(X_test)
\end{Code}

The same estimator participates in a \sklearn pipeline and parameter search.
The scaler illustrates pandas-aware pipeline composition while preserving
predictor order.
%
\begin{Code}
from sklearn.model_selection import GridSearchCV, StratifiedKFold
from sklearn.pipeline import Pipeline
from sklearn.preprocessing import StandardScaler

pipeline = Pipeline([
    ("scale", StandardScaler().set_output(transform="pandas")),
    ("tree", ConditionalInferenceTreeClassifier(**common)),
])
search = GridSearchCV(
    pipeline,
    {
        "tree__alpha_selector": [0.01, 0.05],
        "tree__max_depth": [3, 5],
    },
    cv=StratifiedKFold(5, shuffle=True, random_state=1718),
    scoring="balanced_accuracy",
    n_jobs=1,
)
search.fit(X_train, y_train)
\end{Code}

With the fixed split and random seed above, the tree attained balanced accuracy
0.9795 and area under the receiver operating characteristic curve 0.9774. The
100-tree forest attained 0.9850 and 0.9985, respectively, with an out-of-bag
accuracy of 0.9507. The parameter search selected a Stage A threshold of 0.01
and maximum depth 3, with mean cross-validated balanced accuracy 0.9402. The
replication output includes all feature importances, path and leaf dimensions,
realized permutation counts, cross-validation results, holdout predictions,
and a machine-readable receipt.

\subsection{Statistical components} \label{sec:components}

Association statistics, permutation tests, split criteria, and threshold
generators are separate registered components. The tree implementation resolves
the requested components during fitting and uses the same implementations for
single trees and forests. This organization keeps classification and regression
behavior parallel while registered statistics and threshold methods preserve a
common estimator interface.

Parameter validation occurs before fitting and enforces compatible selector
combinations, valid probability thresholds, feasible sample fractions, and
forest settings with defined outputs. Random seeds are propagated through
permutation tests, sample splitting, bootstrap sampling, and tree construction.
Fitted estimators retain the parameters expected by \sklearn cloning and
model-selection utilities. Table~\ref{tab:software-architecture} summarizes
the responsibilities and interfaces of the main implementation components.

\begin{center}
\small
\begin{tabular}{p{0.22\textwidth}p{0.67\textwidth}}
\hline
Component & Responsibility \\ \hline
Parameter validation & Check selector compatibility, probability thresholds,
sampling fractions, and forest settings before fitting. \\
Nodewise testing & Resolve registered association statistics and permutation
procedures, then retain realized permutation counts. \\
Threshold selection & Generate admissible candidates for the selected
predictor and evaluate registered impurity criteria and permutation tests. \\
Tree recursion & Propagate node samples, active predictors, depth, and fitted
random state while constructing terminal and internal nodes. \\
Forest aggregation & Fit independently seeded trees to resampled observations
and predictor subsets, then aggregate predictions and feature importances. \\
Random seeds & Derive node operations from the estimator seed and
one deterministic seed per forest tree. \\
Model inspection & Expose the fitted tree, terminal-node assignments, sparse
decision paths, feature importances, and realized permutation counts. \\
Extension points & Register new association statistics, permutation tests,
split criteria, and threshold generators behind the common estimator
interface. \\ \hline
\end{tabular}
\captionof{table}{\label{tab:software-architecture} Responsibilities of the
main software components. Registered statistical components are shared by the
classification and regression estimators.}
\end{center}

\subsection{Computation} \label{sec:computation}

Permutation testing dominates runtime when many predictors, thresholds, or
resamples are requested. Performance-critical statistics use
\pkg{Numba}-compiled kernels \citep{Lam+Pitrou+Seibert:2015}. Forest trees can
be fit concurrently, and sequential stopping can reduce permutation work.
Threshold subsampling bounds the number of Stage B candidates, while feature
muting can reduce the predictor set passed to descendants. Each control changes
the realized computation and may change the fitted model. Explicit estimator
parameters make these choices auditable.

\section{Relationship to existing software} \label{sec:comparison}

\pkg{partykit} is the reference software ecosystem for conditional inference
trees and forests in \proglang{R}. It implements the conditional inference
framework through linear statistics and provides a broad object system for
recursive partitioning \citep{Hothorn+Hornik+Zeileis:2006,Hothorn+Zeileis:2015}.
\citrees follows the same high-level separation of variable selection and
split-point optimization with package-specific selector statistics, stopping
rules, forest controls, and fitted object representation. \pkg{partykit}
provides exact \code{ctree} and \code{cforest} behavior, while \citrees serves
native \proglang{Python} pipelines.

\sklearn trees and forests provide substantially faster CART-style fitting for
many prediction tasks and remain essential baselines. Their split search
jointly optimizes a predictor and threshold by impurity reduction. \citrees
incurs the additional cost of nodewise association tests in exchange for
separating predictor screening from threshold optimization. It also exposes
linear and nonlinear selector choices and fixed or sequential permutation
budgets. These implementations answer related but different modeling needs,
and predictive comparisons should retain both.

Table~\ref{tab:software-comparison} summarizes the language, model families,
feature-screening procedure, and estimator interfaces.

\begin{center}
\begin{tabular}{llll}
\hline
Capability & \citrees & \pkg{partykit} & \sklearn \\ \hline
Primary language & \proglang{Python} & \proglang{R} & \proglang{Python} \\
Conditional feature screen & Yes & Yes & No \\
Classification and regression & Yes & Yes & Yes \\
Single trees and forests & Yes & Yes & Yes \\
Nonlinear selector choices & Yes & Framework-dependent & No \\
\sklearn estimator protocol & Yes & No & Yes \\ \hline
\end{tabular}
\captionof{table}{\label{tab:software-comparison} High-level comparison of the software
interfaces relevant to \citrees. ``Conditional feature screen'' denotes a
predictor-response test before threshold optimization. The packages expose
different statistical procedures; behavioral comparisons account for each
procedure and its defaults.}
\end{center}

\section{Evaluation and replication design} \label{sec:evaluation}

The evaluation separates fixed-test calibration, fitted-model behavior, and
computation. The full profile defines every reported estimate. A reduced quick
profile retains the same datasets, metrics, output schemas, and validation
rules for local replication.

\subsection{Null calibration} \label{sec:calibration}

Fixed-budget selector experiments generate predictors independently of the
response. Classification responses contain equal class counts, and regression
responses follow a standard normal distribution. Predictors follow standard
normal, binary, or four-level ordinal distributions. The full profile covers
28 combinations of task, selector, predictor distribution, and sample size,
including the compatible max-$T$ selector combinations. Each combination uses
5,000 replicates and 999 response permutations. The primary estimate is the
proportion of corrected Monte Carlo values below 0.05.

Root-level experiments fit depth-one trees under the global null. Separate
conditions activate the feature gate, the threshold gate, or both gates.
Exhaustive, adaptive, and simple permutation modes are crossed with the
applicable feature- and threshold-scanning controls. The design also varies the
number and distribution of candidate predictors and the threshold search. The
full profile contains 78 conditions with 5,000 replicates per condition.
Root-split frequency measures the fitted decision, while recorded selector and
splitter permutation counts measure the computation used to reach it.

Across the 28 selector conditions, the rejection rate at the 0.05 level ranged
from 0.0356 to 0.0550, with a mean of 0.0473. With 999 permutations and the
add-one correction, the largest attainable rejection level is 0.049. No 95\%
confidence interval lay entirely above the nominal level; five intervals, all
involving binary or four-level ordinal predictors, lay entirely below 0.05,
indicating mild conservatism for coarse predictor distributions. Each estimate
rests on 5,000 replicates and 4,995,000 realized response permutations.

Across the 78 root-level conditions, the split rate under the global null
ranged from 0.0050 to 0.0868, with a mean of 0.0419. Stopping mode separates
the estimates. The 14 exhaustive conditions ranged from 0.0072 to 0.0502, and
the 32 adaptive conditions from 0.0050 to 0.0480, so adaptive stopping never
exceeded the nominal 0.05 level. The 32 simple-stopping conditions ranged from
0.0084 to 0.0868 and exceeded 0.05 in 22 conditions; in 14 of these, the 95\%
confidence interval excluded 0.05 from below. In the 14 matched comparisons
that share a design with an exhaustive condition, adaptive stopping tracked
the exhaustive split rate closely, with a largest absolute difference of
0.012, while using 2.0\% to 6.2\% of the exhaustive permutation count; simple stopping used 2.1\% to
13.6\% of the exhaustive count but inflated the split rate. These results
support adaptive stopping as the default and confine simple stopping to
exploratory use.

\subsection{Variable cardinality} \label{sec:cardinality}

The cardinality experiment uses 200 observations and five independent ordered
discrete predictors with exactly 2, 4, 10, 20, and 200 observed levels. Every
level occurs equally often within a predictor. Values within columns and the
five column positions are shuffled independently in each replicate, and the
response is generated independently of all predictors.

Tree comparisons use 10,000 replicates for classification and regression.
\citrees and \pkg{partykit} use a familywise threshold of 0.05 with Bonferroni
adjustment across the five predictors and a nominal budget of 999 permutations.
\citrees evaluates each candidate with 4,995 permutations to retain resolution
at the adjusted threshold, while \pkg{partykit} uses its native Monte Carlo
distribution with 999 permutations. CART stumps provide the impurity-based
comparison. The analysis retains candidate statistics, corrected permutation
values, native root decisions, ties, and the no-split outcome.

Forest comparisons use 2,500 replicates and 100 depth-one trees per forest.
Every tree receives all five predictors and a bootstrap sample of 200
observations. The comparison includes \citrees forests, \pkg{partykit}
conditional inference forests, and \sklearn random forests. Each implementation
contributes its documented native feature summary: normalized impurity decrease
for \citrees and \sklearn, and unconditional permutation importance with 10
permutations for \pkg{partykit}. Positive associations between cardinality and
selection or importance are tested with 9,999 label randomizations. Holm
adjustment is applied to the prespecified family of 14 tree and forest trend
tests.

Under the null, the conditional inference trees rarely split. \citrees split
in 4.07\% of the 10,000 classification replicates and 4.81\% of the regression
replicates at the familywise 0.05 level; the \pkg{partykit} adjusted rates
were 3.64\% and 4.52\%, with native root decisions at 4.05\% and 4.98\%. CART
stumps, which apply no stopping test, split in every replicate.
Table~\ref{tab:cardinality-holm} reports the prespecified family of 14 trend
tests. Holm adjustment rejected five hypotheses: both forced-winner
classification tree trends, where the predictor with the largest corrected
statistic concentrated on high-cardinality columns for \citrees and
\pkg{partykit} alike; the \sklearn random forest importance trend in both
tasks, with mean Kendall $\tau_b$ of 0.711 for classification and 0.718 for
regression; and the \pkg{partykit} conditional inference forest trend for
regression, whose mean $\tau_b$ of 0.027 is small but consistently positive.
The \citrees conditional inference forest trend was not rejected in either
task (mean $\tau_b$ of 0.021 for classification, Holm-adjusted $p$~value
0.062, and $-0.014$ for regression). Conditional on a tree actually splitting,
no cardinality trend in the winning predictor was rejected for either
conditional inference implementation. The forced-winner rejections therefore
reflect which predictor attains the extreme statistic, not the fitted
decisions, which occur at close to the nominal rate and carry no rejected
cardinality preference for \citrees.

\begin{center}
\begin{tabular}{lllrrl}
\hline
Implementation & Task & Trend tested & Effect & Raw $p$ & Holm $p$ \\ \hline
\citrees tree & Classification & Forced winner & 0.119 & 0.0001 & 0.0014 \\
\citrees tree & Classification & Winner given split & 0.022 & 0.379 & 1.000 \\
\pkg{partykit} tree & Classification & Forced winner & 0.113 & 0.0001 & 0.0014 \\
\pkg{partykit} tree & Classification & Winner given split & 0.019 & 0.400 & 1.000 \\
\citrees tree & Regression & Forced winner & 0.001 & 0.488 & 1.000 \\
\citrees tree & Regression & Winner given split & 0.059 & 0.181 & 1.000 \\
\pkg{partykit} tree & Regression & Forced winner & $-0.002$ & 0.544 & 1.000 \\
\pkg{partykit} tree & Regression & Winner given split & 0.106 & 0.058 & 0.460 \\
\citrees forest & Classification & Importance concordance & 0.021 & 0.0069 & 0.0621 \\
\pkg{partykit} forest & Classification & Importance concordance & 0.005 & 0.292 & 1.000 \\
\sklearn forest & Classification & Importance concordance & 0.711 & 0.0001 & 0.0014 \\
\citrees forest & Regression & Importance concordance & $-0.014$ & 0.952 & 1.000 \\
\pkg{partykit} forest & Regression & Importance concordance & 0.027 & 0.0008 & 0.0080 \\
\sklearn forest & Regression & Importance concordance & 0.718 & 0.0001 & 0.0014 \\ \hline
\end{tabular}
\captionof{table}{\label{tab:cardinality-holm} Prespecified family of 14
cardinality trend tests with Holm adjustment at the 0.05 level. Forced winner
tests the predictor with the smallest corrected $p$~value in every replicate;
winner given split restricts to replicates in which the tree split; importance
concordance is the mean Kendall $\tau_b$ between predictor cardinality and the
native feature summary across forest replicates. Effect is the mean
directional association between cardinality rank and the tested outcome. Raw
$p$~values come from 9,999 joint label randomizations with the add-one rule,
so the smallest attainable raw value is 0.0001.}
\end{center}

\subsection{Matched implementation behavior} \label{sec:behavior}

Matched comparisons use the Breast Cancer Wisconsin Diagnostic classification
data and the \sklearn diabetes regression data. Each dataset is partitioned by
10 repeats of five-fold cross-validation. Trees and forests from \citrees and
\pkg{partykit} receive identical training and evaluation rows and the same
integer seed through their native random-number streams. Structural controls
use maximum depth 3, minimum split size 20, minimum leaf size 7, all predictors,
and 999 nominal response permutations. Forests contain 100 trees and use
full-size bootstrap samples. These controls align the data partitions, stopping
depth, node-size constraints, predictor availability, permutation budget,
bootstrap size, and forest size. Each implementation retains its native
association statistic, threshold criterion, random-number stream, and feature
summary.

Tree comparisons record whether each implementation splits and whether the
root predictors agree when both split. Tree feature summaries count accepted
splits. Forest summaries use normalized impurity decrease for \citrees and
unconditional permutation importance with 10 permutations for \pkg{partykit}.
When both summaries distinguish among predictors, concordance is measured by
Kendall's $\tau_b$ and the Jaccard similarity of the top five sets, including
ties at the fifth position.

Held-out classification comparisons report balanced accuracy, prediction
agreement, probability differences and rank correlation, area under the
receiver operating characteristic curve, and log loss. Regression comparisons
report root mean squared error, mean absolute error, prediction correlation,
and the mean absolute difference between predictions. Every fit is repeated
with the same seed to test exact reproducibility of roots, feature summaries,
probabilities, and predictions. Estimates first average folds within each
partition repeat, then average the 10 repeat means. Percentile intervals use
9,999 bootstrap samples of repeat means. The full design contains 200 paired
dataset, partition, and model-family comparisons.

Both implementations made the same split decision in every tree comparison:
each fit a root split in all 50 fold-level fits per dataset, so
split-decision agreement was 1.000 for both datasets. Given that both split,
they chose the same root predictor in 8\% of classification comparisons
(95\% interval 2\% to 14\%) and 12\% of regression comparisons (4\% to 20\%),
so the matched controls align whether to split far more than where. Feature
summaries agreed more at the forest level than at the tree level. Forest
concordance by Kendall's $\tau_b$ was 0.480 (0.465 to 0.497) for
classification and 0.775 (0.756 to 0.798) for regression, with top-five
Jaccard similarity of 0.616 and 0.793; the corresponding tree values were
0.153 and 0.698.

Held-out predictions from the matched forests were close. Classification
forests agreed on 97.0\% of test predictions (96.7\% to 97.3\%), with
balanced accuracy 0.948 for \citrees and 0.943 for \pkg{partykit}; the
bootstrap interval for the difference, 0.002 to 0.008, excludes zero but is
small on the accuracy scale. Regression forest predictions correlated at
0.978, with root mean squared error 57.3 for \citrees and 57.6 for
\pkg{partykit}. Tree-level predictions differed more, with 91.4\%
classification agreement and a regression prediction correlation of 0.789.
Refitting every model with the same seed reproduced feature summaries,
probabilities, and predictions exactly for both implementations in all 200
comparisons. Distinct algorithmic choices at individual nodes therefore
coexist with matched split decisions, concordant forest summaries, and
deterministic replication.

\subsection{Controlled computation} \label{sec:performance}

Two computational studies time model fitting on identical 32-core cloud
instances. Every fit runs in a fresh process after a small fit of the same
method has removed one-time initialization and, where applicable, JIT
compilation; elapsed time covers model construction and fitting only, and
each library uses its own parallelism. \citrees fits forest trees in parallel
worker processes and divides its compiled thread pool among them; \sklearn
forests use all cores; \pkg{partykit} runs on one core by default and is also
timed with 32 cores. Trees have depth at most 3 with minimum split and leaf
sizes of 20 and 7 in every library, forests have 100 trees, and the
significance level is 0.05 with Bonferroni adjustment across predictors.

The first study uses synthetic Gaussian predictors with signal in the first
five, a reference condition of 1,000 observations and 50 predictors, and
one-factor changes to 250 or 4,000 observations, 10 or 200 predictors, 25 or
500 trees, and 99 or 9,999 permutations. It times four configurations. The
exhaustive procedure spends the full permutation budget at every node with no
early stopping, exact threshold search, and every predictor tested at every
node, and is run in all three libraries so that implementation efficiency is
compared at identical statistical work. The recommended \citrees
configuration is its default: the minimum permutation budget with adaptive
stopping, feature muting and scanning, histogram threshold search with at most
256 candidate thresholds, a Bonferroni-adjusted permutation test of the chosen
threshold, and square-root predictor sampling in forests. \pkg{partykit} is
timed at its own default, which also samples square-root predictors, on one
core and on 32 cores, and additionally with every predictor on 32 cores as the
parallel counterpart of the exhaustive procedure. Classification uses the
multiple correlation selector and regression the Pearson correlation selector.
Every condition is repeated 10 times and summarized by its median and
interquartile range; the study comprises 880 exhaustive cells, 320 recommended
\citrees cells, and 540 \pkg{partykit} cells.

The second study times 100-tree classification forests on the ten real
benchmark datasets with at least 500 observations, from vowel (990
observations, 10 predictors) to letter (20,000 observations, 16 predictors)
and gisette (6,000 observations, 5,000 predictors), and on synthetic Gaussian
data with 20 or 100 predictors at 500 to 32,000 observations. Each library
runs at its default configuration with all 32 cores available, and
\pkg{partykit} is also timed on one core. Two \citrees variants isolate the
threshold test: one drops the Bonferroni adjustment over candidate thresholds
and one omits the threshold test. Each fit is repeated twice, except on
letter, isolet, and gisette, where every configuration is fit once, and no fit
reached the one-hour limit. For
every dataset we also record the mean number of distinct candidate thresholds
per predictor, capped at the 256 the histogram search considers.

Table~\ref{tab:performance-reference} reports the reference condition of the
first study. At identical exhaustive work \pkg{partykit} is the faster
implementation: 286 seconds against 781 seconds for a classification forest on
one core, and 13 seconds with 32 cores. The recommended \citrees configuration
reaches 20 and 18 seconds for the classification and regression forests, 39
and 17 times faster than its own exhaustive procedure and 1.7 and 1.9 times
faster than \pkg{partykit} on one core, but 11 and 10 times slower than
\pkg{partykit} on 32 cores. Single trees favor \citrees at 0.3 seconds
against 2.9 seconds. The permutation budget is the dominant cost of the
exhaustive procedure in both libraries, 83 to 7,686 seconds in \citrees and 34
to 2,800 seconds in \pkg{partykit} between 99 and 9,999 permutations, and
adaptive stopping removes that axis from the recommended configuration, which
sets its own budget. CART in \sklearn fits the reference forests in 0.16 and
0.12 seconds; it performs no permutation test and is included to place the
cost of conditional inference on a familiar scale.

\begin{center}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrrr}
\hline
 & \multicolumn{2}{c}{Classification} & \multicolumn{2}{c}{Regression} \\
Configuration & Tree & Forest & Tree & Forest \\ \hline
\citrees, exhaustive, all predictors & 7.7 [7.6, 7.7] & 781 [779, 782] & 3.0 [3.0, 3.0] & 309 [308, 310] \\
\citrees, recommended & 0.3 [0.3, 2.0] & 20 [19, 21] & 0.3 [0.3, 0.3] & 18 [17, 18] \\
\pkg{partykit}, exhaustive, all predictors & 2.9 [2.9, 2.9] & 286 [286, 286] & 2.9 [2.9, 2.9] & 287 [286, 287] \\
\pkg{partykit}, all predictors, 32 cores & n/a & 13 [13, 13] & n/a & 13 [13, 13] \\
\pkg{partykit}, default, 1 core & n/a & 34 [33, 35] & n/a & 35 [34, 36] \\
\pkg{partykit}, default, 32 cores & n/a & 1.9 [1.8, 2.0] & n/a & 1.9 [1.8, 2.0] \\
\sklearn CART & 0.01 [0.01, 0.01] & 0.16 [0.16, 0.16] & 0.01 [0.01, 0.01] & 0.12 [0.12, 0.12] \\ \hline
\end{tabular}
\captionof{table}{\label{tab:performance-reference} Median fitting time in
seconds with the interquartile range in brackets over 10 repeats at the
reference condition of 1,000 observations, 50 Gaussian predictors, 999
permutations where applicable, and 100 trees. The exhaustive procedure uses the
full permutation budget without early stopping, exact threshold search, and
every predictor at every node and runs on one core in \pkg{partykit}. The
recommended \citrees configuration and the \pkg{partykit} default both sample
square-root predictors in forests. Single
trees have no 32-core condition because \pkg{partykit} parallelizes only
across trees.}
\end{center}

The second study reverses this ordering on real data.
Table~\ref{tab:performance-real} shows that the recommended \citrees
configuration fits the forest faster than \pkg{partykit} on 32 cores on seven
of the ten datasets, by 1.1 to 15 times, and faster than \pkg{partykit} on one
core on every dataset, by 7.9 to 260 times, with the largest margins at the
largest sample sizes: 4.7 seconds
against 70 and 1,218 seconds on letter, 20 against 171 and 3,024 seconds on
isolet, and 34 against 144 and 2,431 seconds on gisette. \pkg{partykit} on 32
cores is faster on gamma, madelon, and vowel. The candidate-threshold column
explains both patterns. \pkg{partykit} runs one Monte Carlo test per node with
a fixed budget of 999 permutations and then chooses the split by maximizing the
statistic, so its cost at every node is the budget times the sample size,
independent of the data. The recommended \citrees configuration stops each
predictor's test as soon as the evidence is decisive and mutes predictors that
have already failed, but it also tests the chosen threshold with a Bonferroni
adjustment over the candidate thresholds present at that node. Predictors
with few distinct values, such as the 16 integer levels of letter, admit few
candidates, so this test stops after tens of permutations and the fixed budget
of \pkg{partykit} is almost entirely wasted work. Continuous predictors admit
the full 256 candidates, the adjusted level is 0.05/256, and the threshold test
alone needs about 5,000 permutations of the sample at every node. The clearest
case is gamma, with nine continuous predictors: 42 seconds with the adjustment
and 3.5 seconds without it. Without the adjustment the recommended
configuration is faster than \pkg{partykit} on 32 cores on nine of the ten
datasets, by 2.4 times on madelon and 19 times on letter, and within 0.3
seconds of it on vowel. Omitting the threshold test entirely changes these
times by at most 0.4 seconds, except on isolet, where it adds 7 seconds.

\begin{center}
\footnotesize
\setlength{\tabcolsep}{2.2pt}
\begin{tabular}{lrrrrrrr}
\hline
 & & & & \multicolumn{2}{c}{\citrees} & \multicolumn{2}{c}{\pkg{partykit}} \\
Dataset & Observations & Predictors & Candidates & Recommended & No adjustment & 1 core & 32 cores \\ \hline
letter & 20,000 & 16 & 16 & 4.7 & 3.7 & 1,218 & 70 \\
gamma & 19,020 & 9 & 256 & 42 & 3.5 & 336 & 15 \\
pendigits & 10,992 & 16 & 100 & 7.5 & 3.0 & 394 & 21 \\
isolet & 7,797 & 616 & 243 & 20 & 6.4 & 3,024 & 171 \\
musk & 6,597 & 166 & 247 & 10 & 3.4 & 478 & 22 \\
gisette & 6,000 & 5,000 & 162 & 34 & 18 & 2,431 & 144 \\
page-blocks & 5,473 & 10 & 241 & 6.8 & 2.6 & 152 & 7.6 \\
spam & 4,601 & 57 & 182 & 5.4 & 2.8 & 206 & 9.5 \\
madelon & 2,000 & 500 & 132 & 21 & 4.4 & 194 & 11 \\
vowel & 990 & 10 & 256 & 3.9 & 2.5 & 41 & 2.2 \\ \hline
\end{tabular}
\captionof{table}{\label{tab:performance-real} Median fitting time in seconds
for 100-tree classification forests on the real benchmark datasets with at
least 500 observations, each library at its default configuration on a 32-core
machine. Candidates is the mean number of distinct candidate thresholds per
predictor, capped at 256. No adjustment is the recommended \citrees configuration
without the Bonferroni adjustment over candidate thresholds in the threshold
test. Medians are over two repeats, or a single fit on letter, isolet, and
gisette.}
\end{center}

Synthetic Gaussian predictors are the continuous case throughout, and
Table~\ref{tab:performance-scaling} shows the consequence as the sample size
grows. The recommended configuration is 1.1 to 2.8 times faster than
\pkg{partykit} on one core at every sample size and 7 to 16 times slower than
\pkg{partykit} on 32 cores. Without the threshold adjustment it is within a
factor of 1.6 of \pkg{partykit} on 32 cores at 2,000 observations and 3.2 to
8.6 times faster at 8,000 and 32,000 observations. Strong signals, with equal
coefficients and half the noise, change the times of the recommended
configuration by at most 16 percent except at 8,000 observations and 20
predictors, where it takes 120 rather than 71 seconds, and change the
\pkg{partykit} times by at most 19 percent. The increase in peak resident
memory over the pre-fit process was at most 49 MB in every condition of the
first study, so memory did not separate the implementations. Together the two studies locate the cost of conditional
inference in how permutations are spent rather than in tree construction:
adaptive stopping, muting, and scanning remove most of the fixed budget that
\pkg{partykit} pays, and the one remaining fixed cost in \citrees, the
adjusted threshold test on continuous predictors, is an option the user
controls.

\begin{center}
\small
\begin{tabular}{rrrrrr}
\hline
 & & \multicolumn{2}{c}{\citrees} & \multicolumn{2}{c}{\pkg{partykit}} \\
Observations & Predictors & Recommended & No adjustment & 1 core & 32 cores \\ \hline
500 & 20 & 12 & n/a & 12 & 0.7 \\
2,000 & 20 & 36 & 2.5 & 51 & 2.6 \\
2,000 & 100 & 34 & 6.5 & 74 & 4.0 \\
8,000 & 20 & 71 & 3.0 & 200 & 9.6 \\
8,000 & 100 & 124 & 4.3 & 306 & 15 \\
32,000 & 20 & 539 & 4.9 & 810 & 42 \\
32,000 & 100 & 554 & 10 & 1,218 & 76 \\ \hline
\end{tabular}
\captionof{table}{\label{tab:performance-scaling} Median fitting time in
seconds over at least two repeats for 100-tree classification forests on synthetic
Gaussian predictors with weak signal in the first five, each library at its
default configuration on a 32-core machine. Columns are as in
Table~\ref{tab:performance-real}; the no-adjustment variant was not run at 500
observations.}
\end{center}

\subsection{DGRP cardiac application} \label{sec:dgrp}

The biomedical application reanalyzes public cardiac measurements from the
\textit{Drosophila} Genetic Reference Panel (DGRP). The source study analyzed
1,956 one-week-old females from 167 sequenced inbred lines
\citep{Saha+Spinelli+CastroMondragon:2022}. The pinned, outlier-filtered
worksheet used here contains 2,032 individual records across the same 167
lines. We compute one mean outcome per line for seven traits after requiring
at least seven recorded individuals: diastolic interval, systolic interval,
heart period, end diastolic diameter, end systolic diameter, fractional
shortening, and arrhythmia index. This rule retains 157 to 166 lines per trait.
End diastolic diameter was specified as the primary trait before the full
profile was run. The other six traits form the secondary family specified
before that run.

The DGRP2 genotypes and Phenosnip covariates come from the associated
ArchiHeart deposit \citep{Saha+Spinelli+CastroMondragon:2021Data}. Genotype
quality control is computed once across its full 205-line panel, which contains
the 167 phenotype-source lines and 38 additional genotype lines. Variants are
retained when their call rate is at least 0.95 and their minor allele frequency
is greater than 0.04. The deposited database supplies 17 numeric covariates
used by the source analysis to encode \textit{Wolbachia} infection and common
polymorphic inversions. For each trait, the eligible lines are assigned to 10
independently shuffled five-fold partitions. Within each training fold,
missing genotypes are imputed by training-set medians. Least-squares
coefficients estimated from the training lines remove the covariate effects
from both the outcome and each genotype in the corresponding training and
evaluation sets.

All retained variants are screened within each training set by the absolute
correlation between the adjusted genotype and adjusted outcome. The 100
highest-scoring variants enter four ranking methods: marginal correlation,
absolute coefficients from ridge regression with penalty 1, a conditional
inference tree, and a 10-tree conditional inference forest. The tree and forest
use the randomized dependence coefficient with 10 random projections and rank
variables by normalized impurity decrease. Both models use squared-error
splitting, 0.05 Bonferroni-adjusted feature and threshold gates, automatic
permutation budgets with adaptive stopping, feature scanning and muting, exact
threshold search, unrestricted depth, minimum split and leaf sizes of 2 and 1,
and the same samples for structure and leaf estimation. The tree passes its
active candidate pool to each node. The forest uses full-size bootstrap
samples, square-root predictor sampling at each node, and one execution thread.
Score ties are resolved by the initial screening order, which is itself
resolved by source variant order.
Each ranking is evaluated with the same ridge regression model at 5, 10, 25,
50, and 100 selected variants. Standardization is estimated from the training
set. Held-out performance is measured by mean squared error and by predictive
$R^2$ relative to the mean adjusted outcome in the training set.

Feature stability is evaluated within each repeat by comparing every pair of
training folds. The exact Jaccard similarity of the top 10 variants was
specified as the primary stability endpoint before the full profile was run.
Secondary summaries use exact sets of 5 and 25 variants and
linkage-disequilibrium-aware sets at all three feature counts. The latter use
maximum-cardinality one-to-one matching between variants on the same chromosome
when their squared correlation is at least 0.8.
Correlations are computed from unadjusted genotype dosages among DGRP lines
shared by the two training sets, using pairwise-complete observations.

The primary inferential contrast is the held-out mean squared error of the
forest ranking minus that of the marginal ranking at 10 selected variants for
end diastolic diameter. Let $d_{ij}$ be this difference for fold $i$ of repeat
$j$, let $s_d^2$ be the sample variance of the 50 fold differences, and let
$\bar{q}$ be the mean evaluation-to-training sample-size ratio. The
lower-tailed corrected repeated-cross-validation statistic is
%
\begin{equation}
  t =
  \frac{\bar{d}}
       {\sqrt{\left(1/50 + \bar{q}\right)s_d^2}},
  \label{eq:dgrp-corrected-cv}
\end{equation}
%
with 49 degrees of freedom \citep{Bouckaert+Frank:2004}. Negative differences
favor the forest ranking. The same contrast is evaluated for the six secondary
traits, and their $p$~values are adjusted together by Holm's method.

\subsection{Replication records} \label{sec:replication}

The quick profile uses fewer simulation replicates, three-fold
cross-validation, smaller permutation and forest budgets, and two timing
repeats. Both profiles record random seeds, software versions, source and input
hashes, platform metadata, raw outputs, and generated artifact hashes. The
top-level replication command validates each component before publishing a
combined output directory.

\section{DGRP application results} \label{sec:dgrp-results}

For the primary end diastolic diameter analysis, mean held-out squared error at
10 selected variants was 166.61 for the forest ranking and 171.15 for the
marginal ranking. The mean difference, forest minus marginal, was $-4.54$
squared micrometers, with a corrected repeated-cross-validation statistic of
$t(49)=-0.297$ and a lower-tailed $p$~value of 0.384. At the same feature
count, the forest ranking had the lowest mean squared error for five of the
seven traits; the tree ranking had the lowest value for end systolic diameter,
and the marginal ranking had the lowest value for arrhythmia index.
Table~\ref{tab:dgrp-inference} reports the contrast specified before the full
profile was run for every trait. The difference was negative for five of the
six secondary traits. All six Holm-adjusted $p$~values were at least 0.849.

\begin{center}
\begin{tabular}{lrrrrrr}
\hline
Trait & Lines & Marginal & Forest & Difference & Raw $p$ & Holm $p$ \\ \hline
EDD & 159 & 171.2 & 166.6 & $-4.540$ & 0.384 & n/a \\
DI  & 165 & 0.05363 & 0.04540 & $-0.008228$ & 0.226 & 1.000 \\
SI  & 166 & 0.0008509 & 0.0007874 & $-0.00006344$ & 0.231 & 1.000 \\
HP  & 165 & 0.06736 & 0.04668 & $-0.02069$ & 0.142 & 0.849 \\
ESD & 157 & 70.02 & 69.25 & $-0.7691$ & 0.459 & 1.000 \\
FS  & 158 & 0.002541 & 0.002507 & $-0.00003372$ & 0.436 & 1.000 \\
AI  & 166 & 0.04017 & 0.04186 & $+0.001686$ & 0.633 & 1.000 \\ \hline
\end{tabular}
\captionof{table}{\label{tab:dgrp-inference} Mean held-out squared error after
selecting 10 variants and the corrected repeated-cross-validation comparison
of the forest and marginal rankings. Difference is forest minus marginal, so a
negative value favors the forest. The raw $p$~values are lower-tailed. The six
secondary traits form the Holm family; n/a identifies the separately tested
primary trait. EDD is end diastolic diameter, DI is diastolic interval, SI is
systolic interval, HP is heart period, ESD is end systolic diameter, FS is
fractional shortening, and AI is arrhythmia index. Squared-error scales are
trait-specific.}
\end{center}

Across the 28 combinations of trait and feature count at 5, 10, 25, and 50
variants, the forest ranking had the lowest mean squared error in 21, the tree
in 4, the marginal ranking in 2, and the ridge ranking in 1. At 100 variants,
all four methods supplied the same candidate set to the downstream model and
therefore produced numerically identical predictions. The mean adjusted
outcome from the training set yielded lower average squared error than every
ranking evaluated with ridge regression. Mean predictive $R^2$ across the 140
configurations ranged from $-4.180$ to $-0.201$; at 10 variants, it ranged from
$-1.163$ to $-0.248$.

The selected variants were also sensitive to the training fold. Across 100
fold pairs for each trait and method, mean exact top-10 Jaccard similarity
ranged from 0.0058 to 0.0790. Linkage-disequilibrium-aware matching gave a
range of 0.0081 to 0.0811. For end diastolic diameter, exact top-10 similarity
was 0.0177 for the marginal ranking, 0.0070 for ridge, 0.0123 for the tree, and
0.0080 for the forest; the corresponding linkage-disequilibrium-aware values
were 0.0205, 0.0081, 0.0128, and 0.0090. The variation across folds makes the
ranked variants hypotheses that require evaluation in independent data.

\section{Discussion} \label{sec:discussion}

\citrees makes conditional-inference-style trees and forests available as
native \proglang{Python} estimators. Its main practical advantage is explicit
control over the nodewise association test, multiplicity threshold,
permutation computation, and threshold search while retaining compatibility
with common model-selection workflows. Its main cost is computation:
permutation tests can be much slower than impurity-only split search,
especially in high dimensions or large forests.

The package serves applications where the separation between feature screening
and threshold optimization is part of the modeling goal. \pkg{partykit} serves
analyses requiring exact \proglang{R} reference behavior. Inferential analysis
quantifies fixed-node permutation calibration, while adaptive tree construction
produces fitted predictive models. Applications should evaluate predictive
performance, feature stability, and runtime against both conditional inference
and CART-style alternatives.

The DGRP application demonstrates the package in genome-scale screening within
each training fold. Conditional forest rankings frequently reduced held-out
squared error relative to the other rankings. The training-set mean baseline
had lower average error than every fitted model, and selected feature sets
varied substantially across folds. Researchers using ranked features should
pair the ordered list with held-out prediction and stability summaries.

\subsection{Limitations}

The DGRP estimates describe one public panel with 157 to 166 eligible lines per
trait. The fixed genotype filters, covariates, 100-variant screen, and ridge
downstream model define the reported comparison. Independent cardiac panels
are needed to assess the transportability of individual variants and ranking
advantages. More generally, permutation testing makes \citrees substantially
more computationally intensive than impurity-only trees, particularly for
large forests and high-dimensional predictors.

\section{Software and data availability} \label{sec:availability}

Version 0.1.0 of \citrees, the article source, and the replication programs are
available at \url{https://github.com/rmill040/citrees} under the MIT license.
The package requires \proglang{Python} 3.12 or later. The locked
\proglang{Python} environment and the reference container specification are
included in the repository. The reference container pins \proglang{R} 4.5.2,
\pkg{partykit} 1.2-24, and the numerical packages used by the
\proglang{Python}--\proglang{R} bridge.

The top-level replication command provides smoke, quick, and full profiles.
Each execution writes to a new result directory and validates every component
before publishing the combined result tree. The full profile requires a clean
source tree at the beginning and throughout execution. Component receipts
record the profile, random seed, source revision, software versions, platform,
elapsed time, source and input hashes, and output hashes. Distributed
calibration and behavioral comparisons additionally validate complete shard
assignment and preserve realized cloud-compute accounting.

Simulation inputs are generated from recorded seeds. The tutorial data are
distributed with \sklearn. The DGRP workflow acquires the cited phenotype,
genotype, and covariate sources
\citep{Saha+Spinelli+CastroMondragon:2022,
Saha+Spinelli+CastroMondragon:2021Data}. It verifies pinned file sizes and
cryptographic hashes before extraction and records the validated input hashes
in the analysis receipt. A configurable shared data directory permits the
validated DGRP inputs to be reused across replication profiles.

\section*{Acknowledgments}

The authors acknowledge Amazon Web Services for supporting this work.

\bibliography{refs}

\end{document}
